"""External (non-official) search tools for IP-MCP — Phase 1B.

⚠ This package is COMPLETELY INDEPENDENT from ip_mcp.tools_official.
   Do not add imports between the two. The MCP server registers tools
   from each side separately so the boundary stays explicit.
"""
